<nav class="navbar navbar-expand-lg navbar-dark bg-primary" style="margin-bottom: 30px;">
    <div class="container-lg">
        <a class="navbar-brand" href="{{ path('/') }}">OTUS-Highload</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0 flex-grow-1">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path('/') }}">Главная</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Несуществующая страница</a>
                </li>
            </ul>

            <a class="btn text-light" href="{{ path('auth/register') }}">Регистрация</a>
            <a class="btn btn-light" href="{{ path('auth/login') }}">Вход</a>
        </div>
    </div>
</nav>